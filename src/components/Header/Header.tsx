const Header = () => {
  return (
    <header className="fixed h-24 border-b w-full flex justify-center items-center font-bold text-5xl backdrop-blur-md bg-white/50">
      Country
    </header>
  );
};

export default Header;
